{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c \"if [ -d backend ]; then cd backend; fi && chmod +x deploy.sh && ./deploy.sh && gunicorn core.wsgi:application --bind 0.0.0.0:$PORT\"",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
} 